# TODO
- implémenter des tests à vérifier le fonctionnement des représentations numériques
- implémenter des tests à vérifier le jumelage des représentations numériques sur les distances de compression
- implémenter des tests pour vérifier le bon fonctionnement des algorithmes de compression
# Comment effectuer les tests
`$/src python3 -m unittest discover -s ./tests/`